"use client";

import { ReactTyped } from "react-typed";

import React from "react";

const Typer: React.FC = () => {
  return <ReactTyped strings={["Bem vindo à TV 3.0"]} typeSpeed={40} />;
};

export default Typer;
